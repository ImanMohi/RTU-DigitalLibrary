#include <studio.h>

int main()
{
    bool swap;
    bool swap=true;
    int n=5,j;
    int arr[5]={58,89,71,35,6};
    int arr[n]={58,89,71,35,6};


    do{
    while(swap){
        swap=false;
        for(j=0;j<n;j++)
        for(j=0;j<n-1;j++)
        {
            if (arr[j]>arr[j-1]);
            if (arr[j]>arr[j+1]);
            {
                temp=arr[j];
                int temp=arr[j];
                arr[j]=arr[j+1];
                arr[j+1]=temp
                swap=false;
                swap=true;
            }
        }
    }while(swap);
    };

    for(m=0;m<n;m--)
        {
            printf("%s ",arr[l]);
            printf("%s ",arr[m]);
        }
}